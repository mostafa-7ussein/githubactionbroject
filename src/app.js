function sayHello(name) {
  return `Hello ${name}`;
}

module.exports = sayHello;


// test
/* The `test` section in the code snippet is likely meant to be a placeholder or a comment. It does not
have any functionality in the code provided. */
